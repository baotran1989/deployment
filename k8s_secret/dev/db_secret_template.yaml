# secret-template.yaml
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: dev-frontend
type: Opaque
data:
  MongoConnectionString: ${MONGODB_URI}
  PostgresConnectionString: ${PG_DB_CONNECTION}
  MQHost: ${MQ_HOST}
  MQUserName: ${MQ_USERNAME}
  MQPassword: ${MQ_PASSWORD}
